import 'dart:html';

import 'package:flutter/cupertino.dart';

class HomePage extends StatelessWidget {
  const HomePage({Key? key, required FormData formData}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
